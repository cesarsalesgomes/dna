overwrite: true
schema:
  - http://localhost:8055/graphql?access_token=${DIRECTUS_ACCESS_TOKEN}
documents: null
generates:
  # Backend Schema
  ./graphql-schema.ts:
    plugins:
      - 'typescript'

  # Frontend Schema
  ../dna-frontend/graphql-schema.ts:
    plugins:
      - 'typescript'
